<UserControl x:Class="D2G.Iris.ML.ConfigUI.WPF.Views.VisualisationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="http://schemas.abtsoftware.co.uk/scichart"
             xmlns:converters="clr-namespace:D2G.Iris.ML.ConfigUI.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Generate Correlation Heatmap" Command="{Binding GenerateChartCommand}" 
                    Width="180" Height="30" Margin="0,0,10,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="10">
            <s:SciChartSurface x:Name="ChartSurface" 
                               Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                
                <s:SciChartSurface.XAxis>
                    <s:NumericAxis x:Name="XAxis" 
                                   AxisTitle="{Binding XAxisTitle}"
                                   AutoRange="Always"/>
                </s:SciChartSurface.XAxis>
                
                <s:SciChartSurface.YAxis>
                    <s:NumericAxis x:Name="YAxis" 
                                   AxisTitle="{Binding YAxisTitle}"
                                   AutoRange="Always"/>
                </s:SciChartSurface.YAxis>
                
                <s:SciChartSurface.ChartModifier>
                    <s:ModifierGroup>
                        <s:ZoomPanModifier />
                        <s:MouseWheelZoomModifier />
                        <s:ZoomExtentsModifier />
                        <s:CursorModifier />
                    </s:ModifierGroup>
                </s:SciChartSurface.ChartModifier>
                
            </s:SciChartSurface>
            
            <TextBlock Text="Click 'Generate Correlation Heatmap' to analyze relationships between numeric fields"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       FontSize="16" 
                       Foreground="Gray">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding HasChartData}" Value="False"/>
                                    <Condition Binding="{Binding IsLoading}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
                       
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" 
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                    <Ellipse x:Name="Dot1" Width="12" Height="12" Fill="#007ACC" Margin="0,0,8,0">
                        <Ellipse.Resources>
                            <Storyboard x:Key="BounceStoryboard1" RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                               From="0" To="-20" Duration="0:0:0.6" 
                                               AutoReverse="True" BeginTime="0:0:0"/>
                            </Storyboard>
                        </Ellipse.Resources>
                        <Ellipse.RenderTransform>
                            <TranslateTransform/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard Storyboard="{StaticResource BounceStoryboard1}"/>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                    
                    <Ellipse x:Name="Dot2" Width="12" Height="12" Fill="#007ACC" Margin="0,0,8,0">
                        <Ellipse.Resources>
                            <Storyboard x:Key="BounceStoryboard2" RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                               From="0" To="-20" Duration="0:0:0.6" 
                                               AutoReverse="True" BeginTime="0:0:0.2"/>
                            </Storyboard>
                        </Ellipse.Resources>
                        <Ellipse.RenderTransform>
                            <TranslateTransform/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard Storyboard="{StaticResource BounceStoryboard2}"/>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                    
                    <Ellipse x:Name="Dot3" Width="12" Height="12" Fill="#007ACC">
                        <Ellipse.Resources>
                            <Storyboard x:Key="BounceStoryboard3" RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                               From="0" To="-20" Duration="0:0:0.6" 
                                               AutoReverse="True" BeginTime="0:0:0.4"/>
                            </Storyboard>
                        </Ellipse.Resources>
                        <Ellipse.RenderTransform>
                            <TranslateTransform/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard Storyboard="{StaticResource BounceStoryboard3}"/>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </StackPanel>
                
                <TextBlock Text="{Binding LoadingMessage}" FontSize="14" FontWeight="Medium"
                           HorizontalAlignment="Center" TextAlignment="Center" Foreground="#333"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>