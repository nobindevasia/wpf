<UserControl x:Class="D2G.Iris.ML.ConfigUI.WPF.Views.DatabaseSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">

    <GroupBox Header="Database Settings" Margin="10">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Server -->
            <Label Grid.Row="0" Grid.Column="0" Content="Server:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" 
                     Text="{Binding Server, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Height="25" Margin="0,0,10,0"/>

            <!-- Database -->
            <Label Grid.Row="2" Grid.Column="0" Content="Database:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" 
                     Text="{Binding Database, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Height="25" Margin="0,0,10,0"/>

            <!-- Table Name -->
            <Label Grid.Row="4" Grid.Column="0" Content="Table Name:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="4" Grid.Column="1" 
                     Text="{Binding TableName, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Height="25" Margin="0,0,10,0"/>

            <!-- Output Table Name -->
            <Label Grid.Row="6" Grid.Column="0" Content="Output Table Name:" VerticalAlignment="Center"/>
            <TextBox Grid.Row="6" Grid.Column="1" 
                     Text="{Binding OutputTableName, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Height="25" Margin="0,0,10,0"/>

            <!-- Where Clause -->
            <Label Grid.Row="8" Grid.Column="0" Content="Where Clause:" VerticalAlignment="Top" Margin="0,5,0,0"/>
            <TextBox Grid.Row="8" Grid.Column="1" 
                     Text="{Binding WhereClause, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Auto" Height="60" Margin="0,0,10,0"/>

            <!-- Test Connection Button -->
            <Button Grid.Row="10" Grid.Column="1" 
                    Content="Test Connection"
                    Command="{Binding TestConnectionCommand}"
                    HorizontalAlignment="Left"
                    Width="120" Height="30"
                    Background="LightBlue"/>
        </Grid>
    </GroupBox>
</UserControl>